@AGENTS.md

## Claude Code Notes

- Treat `AGENTS.md` as the shared project source of truth.
- Keep this file short. Put durable project instructions in `AGENTS.md`.
- Personal local notes belong in `CLAUDE.local.md`, which is gitignored.
- When the user asks for a code change, make the change, run the relevant verification command, and update docs when behavior changes.

